server:
  port: 6004

serverUrl: http://localhost:8080/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka

info:
  app:
    name: ${spring.application.name}

management:
  endpoints:
    web:
      exposure:
        include: 'prometheus'