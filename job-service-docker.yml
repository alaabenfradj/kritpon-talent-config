server:
  port: 6002

eureka:
  client:
    service-url:
      defaultZone: http://registry-service:8081/eureka

spring:
  datasource:
    url: jdbc:mysql://job_db:3320/job-service-db
    username: root
    password: root
